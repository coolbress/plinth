# Keeps the commit-SHA pins on GitHub Actions current. The repository requires
# SHA pinning, so these are the only version bumps a workflow file ever gets.
version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    commit-message:
      prefix: "ci(deps)"
